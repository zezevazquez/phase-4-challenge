<% include partials/header %>
<% include partials/navbar %>

<div class="container">
  <div class="row">
    <div class="col">
      <img src="https://f4.bcbits.com/img/a3476070115_10.jpg" class="img-fluid" alt="Responsive image">
    </div>
    <div class="col">
      <h1>Album: <%=reviews[0].title%>!</h1>
      <br>
      <h3>Name: <%=reviews[0].name%></h3>
      <br />
      <a href="/albums/<%=reviews[0].id%>/reviews/new" >New Post!</a>
      <div class="review-feed">
        <% reviews.forEach((review) => { %>
          <div class="row">
            <div class="col">
              <span>Album Title: <a href="/albums/<%= review.album_id %>"><%=review.title%></span></h1></a>
              <br />
              <span>Album Review: <%=review.review_text%></span>
              <br />
              <span>Description: <%=review.review_date%></span>
              <br />
            </div>
            <div class="col">
            <% if (user.id === review.user_id) { %>
              <form action="/albums/<%= review.id %>/<%=user.id%>/delete" method="GET">
                <button class="btn-floating right" type="submit"><i class="material-icons right fa fa-trash" onclick="return confirm('Are you sure you want to delete this review?')"></i></button>
              </form>
            <% } %>
            </div>
          </div>
          <br />
          <br />
          <% }) %>
        </div>
    </div>
  </div>
</div>


<% include partials/footer %>
